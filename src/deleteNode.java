public class deleteNode {
    /**
     * 单链表如果想要删除一个结点
     * 一般来说首先要获取该节点的前面那个结点然后改变指针的指向并把内存释放掉
     * 这个题只知道要被删除的结点又是单链表无法取到前面的结点
     * 那么我们想办法把该结点变成该结点的下个结点
     * @param node
     */
//    public void deleteNode(ListNode node) {
//        node.val = node.next.val;
//        node.next = node.next.next;
//    }
}
